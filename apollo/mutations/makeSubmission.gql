mutation(
  $title: String!
  $userId: ID!
  $createdDate: String!
  $yearId: ID!
  $faculty: String!
  $article: MediaInput!
  $picture: MediaInput
) {
  makeSubmission(
    title: $title,
    userId: $userId,
    createdDate: $createdDate,
    yearId: $yearId,
    faculty: $faculty,
    article: $article
    picture: $picture
  ){
    _id
  }
}
